<resources>

    <style name="BottomNavigationViewIconSize" parent="@style/Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="itemIconSize">36dp</item> <!-- Adjust the size as needed -->
        <item name="android:color">@color/purple</item>
        <item name="android:width">40dp</item> <!-- Adjust the width as needed -->
        <item name="android:height">40dp</item> <!-- Adjust the height as needed -->
        <item name="android:shape">oval</item> <!-- Make the shape circular -->
    </style>

</resources>
